# 1.0.1

Restructured commit history to place TODO comments immediately before the code that resolves them.

# 1.0.0

Initial progressive-history release of the Stencil Subcategories example lab.

- Adopt the progressive-history structure: `main` is the traditional branch, and the tutorial-shaped step history is rebuilt as an independent commit chain tagged `1.0.0` at its tip.
- **Add Subcategories Listing** exercise (prefix `lab`): render a subcategory listing on the category page, build the listing component, add SCSS styles, and expose theme config settings for those styles.
- Based on the Cornerstone base framework version `6.17.0` (anchored by the `framework-6.17.0-4` tag).
